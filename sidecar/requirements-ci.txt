# Jura Trace — CI / PyInstaller build baseline (fully-pinned mirror)
#
# This file is the fully-pinned equivalent of requirements.txt for use in CI
# and PyInstaller builds. It must remain in sync with requirements.txt —
# any package present in requirements.txt must appear here too, at the same
# version. The recurrence guard at scripts/check_requirements_sync.py
# enforces this in CI (wired into .github/workflows/ci.yml).
#
# Relationship to the requirements split:
#   requirements.txt      — runtime baseline (source of truth for versions)
#   requirements-build.txt — adds torch + open_clip for ONNX export + training
#   requirements-ci.txt   — this file; fully-pinned mirror of requirements.txt
#                           used for CI PyInstaller builds
#
# Heavy optional dependencies excluded here (and from requirements.txt):
#   open-clip-torch (~2 GB PyTorch stack) — moved to requirements-build.txt
#   faster-whisper / ctranslate2         — v1.0 deferred (JTV-138)
#   invisible-watermark                  — v1.1; see jura-sidecar.spec comment
#
# All excluded packages are lazy-loaded at runtime and degrade gracefully.
# Transitive dependencies are pinned explicitly to prevent version drift
# from breaking the frozen PyInstaller binary.
fastapi==0.141.1
uvicorn[standard]==0.34.0
httpx==0.28.1
pydantic-settings==2.15.0
python-multipart==0.0.32
starlette==1.6.0
h11==0.16.0
anyio==4.15.1
sniffio==1.3.1
certifi==2026.2.25
Pillow==12.3.0
imagehash==4.3.2
numpy==2.2.4
opencv-python-headless==4.11.0.86
scikit-learn==1.8.0
scikit-image==0.26.0
scipy==1.17.1
# invisible-watermark removed from CI bundle 2026-05-21 — see jura-sidecar.spec
# for rationale. Watermark feature gated behind V1_SHOW_WATERMARK=false in
# frontend; backend code stays in tree for v1.1 unhide. Library remains in
# requirements.txt for dev testing.
pillow-heif==1.6.0
# AVIF support added rc.31 (2026-06-09) — see requirements.txt for rationale.
pillow-avif-plugin==1.5.5
python-dotenv==1.2.3
pydantic==2.11.3
# ONNX runtime — required for CLIP/UnivFD AI detection (JTV-143, 3 May 2026).
# Without this pin the sidecar binary has no onnxruntime and clip_detect is
# always false on CI-built Windows and Linux installers. Pinned to match
# requirements.txt exactly — bump both files together.
onnxruntime==1.24.4
# regex — PCRE-compatible pattern library required by clip_tokenizer.py
# (imported as `import regex as re`). Previously a transitive dep via
# open_clip_torch; that package is in requirements-build.txt not here, so
# regex must be explicit to land in the frozen binary.
regex==2025.10.23
