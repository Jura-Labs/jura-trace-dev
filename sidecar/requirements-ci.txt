# CI-safe requirements — excludes heavy optional dependencies
# open-clip-torch (~2 GB PyTorch), faster-whisper
# All excluded packages are lazy-loaded at runtime and degrade gracefully
#
# Transitive dependencies are pinned explicitly to prevent version
# drift from breaking the frozen PyInstaller binary.
fastapi==0.115.12
uvicorn[standard]==0.34.0
httpx==0.28.1
pydantic-settings==2.13.1
python-multipart==0.0.20
starlette==0.46.2
h11==0.16.0
anyio==4.12.1
sniffio==1.3.1
certifi==2026.2.25
Pillow==11.2.1
imagehash==4.3.2
numpy==2.2.4
opencv-python-headless==4.11.0.86
scikit-learn==1.8.0
scikit-image==0.26.0
scipy==1.17.1
# invisible-watermark removed from CI bundle 2026-05-21 — see jura-sidecar.spec
# for rationale. Watermark feature gated behind V1_SHOW_WATERMARK=false in
# frontend; backend code stays in tree for v1.1 unhide. Library remains in
# requirements.txt for dev testing.
pillow-heif==1.2.0
python-dotenv==1.1.0
pydantic==2.11.3
